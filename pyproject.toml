[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "multiverse-bank-validation"
version = "1.0.0"
description = "Bank-grade paired benchmark for Multiverse Pulsar and HyperNova models"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["benchmark"]

